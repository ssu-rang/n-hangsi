import type { Metadata } from "next";import "./globals.css";export const metadata:Metadata={title:"N행시 — 한 글자씩, 웃음이 이어지는 곳",description:"랜덤 단어와 오늘의 단어로 함께 웃는 N행시 커뮤니티",openGraph:{title:"N행시",description:"한 글자씩, 웃음이 이어지는 곳",images:["/og.png"]},twitter:{card:"summary_large_image",images:["/og.png"]}};export default function Layout({children}:{children:React.ReactNode}){return <html lang="ko"><body>{children}</body></html>}